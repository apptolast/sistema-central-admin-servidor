{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "image-refresh.serviceAccountName" . }}
  labels:
    {{- include "image-refresh.labels" . | nindent 4 }}
{{- end }}
