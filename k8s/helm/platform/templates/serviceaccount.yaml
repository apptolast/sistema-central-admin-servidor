{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "apptolast-platform.serviceAccountName" . }}
  labels:
    {{- include "apptolast-platform.labels" . | nindent 4 }}
automountServiceAccountToken: true
{{- end }}
